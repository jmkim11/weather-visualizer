# 날씨 시각화 (Weather Visualizer)

Python Flask를 활용한 날씨 정보 조회 웹 애플리케이션입니다.

## 실행 방법

1. 필수 패키지 설치:
   ```bash
   pip install -r requirements.txt
   ```
2. 앱 실행:
   ```bash
   python app.py
   ```
3. 브라우저 접속: `http://localhost:5001`
